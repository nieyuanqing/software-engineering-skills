spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    root: INFO
    # 替换为本工程实际包名，如 com.example.<SERVICE_NAME>
    com.example: DEBUG

springdoc:
  swagger-ui:
    enabled: true
  api-docs:
    enabled: true
