# upstream 定义放在这里。只有需要负载均衡到多个后端实例的服务才需要声明 upstream，例如：
#
# upstream <service-name> {
#     server 127.0.0.1:<port1>;
#     server 127.0.0.1:<port2>;
#     keepalive 32;
# }
#
# 单实例服务直接在 vhost 里 proxy_pass 到 http://127.0.0.1:<port>/ 即可，不需要在这里声明。
